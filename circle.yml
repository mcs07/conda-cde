checkout:
  post:
    - ./scripts/checkout_merge_commit.sh

machine:
  services:
    - docker

dependencies:
  override:
    - docker pull pelson/obvious-ci:latest_x64

test:
  override:
    - ./scripts/run_docker_build.sh
