{% set name = "ChemDataExtractor" %}
{% set version = "1.2.0" %}
{% set sha256 = "67f396d96358dbcff48c7d42fd70363d36cb5ca461789f78f7d7e026e9d82ba4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools
  run:
    - python
    - appdirs
    - beautifulsoup4
    - click
    - cssselect
    - dawg
    - lxml
    - nltk
    - pdfminer.six
    - python-crfsuite
    - python-dateutil
    - pyyaml
    - requests
    - six

test:
  imports:
    - chemdataextractor
  commads:
    - cde --help

about:
  home: http://chemdataextractor.org
  license: MIT
  license_file: LICENSE
  summary: 'Automatically extract chemical information from scientific documents'
  doc_url: http://chemdataextractor.org/docs
  dev_url: https://github.com/mcs07/ChemDataExtractor

extra:
  recipe-maintainers:
    - mcs07
