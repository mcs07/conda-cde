{% set name = "ChemDataExtractor" %}
{% set version = "1.2.2" %}
{% set sha256 = "de1fccdc1b495cf97e1afd4fb64f769ee0ea2ab240e20b6c90eac9a0e98a5f4e" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools
  run:
    - python
    - appdirs
    - beautifulsoup4
    - click
    - cssselect
    - dawg
    - lxml
    - nltk
    - pdfminer.six
    - python-crfsuite
    - python-dateutil
    - pyyaml
    - requests
    - six

test:
  imports:
    - chemdataextractor
  commads:
    - cde --help

about:
  home: http://chemdataextractor.org
  license: MIT
  license_file: LICENSE
  summary: 'Automatically extract chemical information from scientific documents'
  doc_url: http://chemdataextractor.org/docs
  dev_url: https://github.com/mcs07/ChemDataExtractor

extra:
  recipe-maintainers:
    - mcs07
